#include "swap.h"
#include "./srcs/Libft/libft.h"

int main(int argc, char **argv)
{
    //t_stack *a;
    //t_stack *b;

    if (argc == 1)
        ft_error(1, "Inserta números como argumentos");
    argv[0] = NULL;
    return(0);
}